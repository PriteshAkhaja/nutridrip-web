Photography for the public site.

`ImageSlot` checks for the file on the server at render time and falls back to
the design pack's striped placeholder when it is missing — so an absent image
never breaks the layout, and adding one needs no code change.

| File | Where it appears | Suggested size |
|---|---|---|
| `home-hero.png` | Home page hero, beside the headline | 1402 × 1122, landscape (in place) |
